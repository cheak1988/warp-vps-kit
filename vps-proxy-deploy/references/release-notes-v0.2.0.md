# v0.2.0 Release Notes

This release turns the repo into a skill-first project.

## What changed

- `vps-proxy-deploy/` is now the canonical product.
- `SKILL.md` is concise and designed for AI use.
- Detailed docs moved into `references/`.
- Deterministic helpers moved into `scripts/`.
- README now explains how to install the skill into Codex and ask AI to run the workflow.

## Positioning

Positioning:

> An AI skill that guides users through recovering and accelerating cheap VPS proxies with Cloudflare CDN, WARP proxy mode, and Xray.
